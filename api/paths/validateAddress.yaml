x-swagger-router-controller: ping
get:
  responses:
    200:
      description: OK
      schema:
        $ref: '#/definitions/Object'
    default:
          description: Error
          schema:
            $ref: "#/definitions/Error"

